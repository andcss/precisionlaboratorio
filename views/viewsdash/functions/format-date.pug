- function formatDate(date) {
- var d = new Date(date);
- var dformat = dformat = [d.getDate(),d.getMonth()+1,d.getFullYear()].join('/')+' '+[d.getHours(),d.getMinutes()].join(':');
- return dformat;
- }
